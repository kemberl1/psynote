# RAG service dependencies — Этап 1 (каркас).
# Только то, что нужно для рабочего /health и будущей структуры.
# Тяжёлые ML-зависимости закомментированы, чтобы образ собирался быстро.

# --- Web framework (каркас) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Vector DB client (нужен с этапа 2; лёгкий, оставляем) ---
qdrant-client==1.12.1

# --- HTTP client (для вызовов LLM «План Б» / межсервисного обмена) ---
httpx==0.28.1

# --- Future (этап 2+): тяжёлые ML-зависимости. Раскомментировать при реализации. ---
# sentence-transformers==3.3.1   # локальные эмбеддинги e5 (docs/03 §3)
# torch                          # бэкенд для sentence-transformers (большой!)
# python-docx==1.1.2             # извлечение текста из .docx (docs/03 §5)
# odfpy==1.4.1                   # извлечение текста из .odt (docs/03 §5)
