# RAG service dependencies — Этап 3 (ingestion корпуса → Qdrant).
# Образ потяжелел за счёт ML-зависимостей (torch, sentence-transformers) — это
# ожидаемо для Этапа 3 (локальные эмбеддинги e5, docs/03 §3).

# --- Web framework (каркас) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18  # FastAPI file upload support (Этап 10, POST /ingest)

# --- Vector DB client (docs/05 §3) ---
qdrant-client==1.12.1

# --- HTTP client (вызов анонимайзера gateway /api/v1/anonymize, docs/04 §1) ---
httpx==0.28.1

# --- LLM X5 CoPilot (OpenAI-совместимый) + ретраи (Этап 4, docs/03 §9–10) ---
# openai SDK указывает на base_url X5; tenacity — экспоненциальный backoff
# внутри одной модели перед фолбэком на следующую.
openai==1.59.6
tenacity==9.0.0

# --- Парсеры форматов корпуса (docs/03 §5) ---
python-docx==1.1.2     # извлечение текста из .docx
odfpy==1.4.1           # извлечение текста из .odt
openpyxl==3.1.5        # извлечение из .xlsx (листы назначений, опц. --include-tables)

# --- Локальные эмбеддинги e5 (docs/03 §3) — медтекст не уходит в облако ---
# CPU-сборка torch (без CUDA) ставится с дефолтного индекса PyPI.
sentence-transformers==3.3.1
torch==2.5.1
